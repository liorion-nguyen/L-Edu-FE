export enum TypeDiscount {
  PERCENT = "PERCENT",
  VALUE = "VALUE",
}

export enum Currency {
  USD = "USD",
  VND = "VND",
}

export enum Status {
  ACTIVE = "ACTIVE",
  INACTIVE = "INACTIVE",
  ARCHIVED = "ARCHIVED",
}

export enum Mode {
  OPEN = "OPEN",
  CLOSE = "CLOSE",
}