export enum TypeDiscount {
  PERCENTAGE = "PERCENTAGE",
  FIXED = "FIXED",
}

export enum Currency {
  USD = "USD",
  VND = "VND",
}

export enum Status {
  ACTIVE = "ACTIVE",
  INACTIVE = "INACTIVE",
  ARCHIVED = "ARCHIVED",
}

export enum Mode {
  OPEN = "OPEN",
  CLOSE = "CLOSE",
}